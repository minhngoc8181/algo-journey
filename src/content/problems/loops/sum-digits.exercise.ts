import { defineExercise } from '../../_loader';
export default defineExercise({ id: 'sum-digits', version: 1, title: 'Sum of Digits', summary: 'Compute the sum of digits of a positive integer.', topic: 'loops', difficulty: 'easy', tags: ['math', 'modulo'], estimatedMinutes: 10, order: 204, mode: 'function_implementation', hints: ['Use a `while` loop that runs as long as `number > 0`.', 'Extract the last digit using `number % 10`. Add it to your running sum.', 'Remove the last digit from the number using `number / 10`.', 'Repeat until the number becomes 0, then return the sum.'], learningGoals: ['Use modulo and integer division'], statement: 'Given a non-negative integer `n`, return the sum of its digits.', constraints: ['0 ≤ n ≤ 10^9'], examples: [{ input: 'n = 123', output: '6' }, { input: 'n = 0', output: '0' }], starter: { file: 'Solution.java', code: `import java.util.*;\n\nclass Solution {\n    int sumDigits(int n) {\n        // Write your code here\n        return 0;\n    }\n}` }, requiredStructure: { className: 'Solution', methodName: 'sumDigits', signature: 'int sumDigits(int n)' }, evaluation: { comparator: 'exact_json' } });
